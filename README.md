Binary executable has been submitted via canvas

To run from here: Clone this repo, Double click the build-VisualStudio2022 to generate a solution file, double click raylib-quickstart.sln to open it in Visual Studio 2022 and run from there.
